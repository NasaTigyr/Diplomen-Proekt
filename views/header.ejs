<!-- Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Martial Arts Competitions</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link <%= currentPage === 'home' ? 'active' : '' %>" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= currentPage === 'events' ? 'active' : '' %>" href="/events">Events</a>
                </li>
                
                <% if (user) { %>
                    <!-- Logged in navigation options -->
                    
                    <% if (user.user_type === 'coach') { %>
                    <!-- Coach-specific navigation -->
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'createEvent' ? 'active' : '' %>" href="/createEvent">Create Event</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'manageClub' ? 'active' : '' %>" href="/manageClub">Manage Club</a>
                    </li>
                    <% } %>
                    
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <%= user.first_name %> <%= user.last_name %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="/profile.ejs">Profile Settings</a></li>
                            
                            <% if (user.user_type === 'regular') { %>
                            <!-- Regular user options -->
                            <li><a class="dropdown-item" href="/createClub">Create Club</a></li>
                            <li><a class="dropdown-item" href="/joinClub">Join a Club</a></li>
                            <% } %>
                            
                            <% if (user.user_type === 'coach') { %>
                            <!-- Coach options -->
                            <li><a class="dropdown-item" href="/getAthletes">View Athletes</a></li>
                            <li><a class="dropdown-item" href="/manageClub">Manage Club</a></li>
                            <% } %>
                            
                            <% if (user.user_type === 'athlete') { %>
                            <!-- Athlete options -->
                            <li><a class="dropdown-item" href="/myClub">My Club</a></li>
                            <li><a class="dropdown-item" href="/myRegistrations">My Competitions</a></li>
                            <% } %>
                            
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">Logout</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <!-- Guest navigation options -->
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'login' ? 'active' : '' %>" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= currentPage === 'register' ? 'active' : '' %>" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
